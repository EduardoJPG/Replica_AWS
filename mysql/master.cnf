[mysqld]
server-id                  = 1
log_bin                    = mysql-bin
binlog_format              = ROW
gtid_mode                  = ON
enforce_gtid_consistency   = ON
binlog_do_db               = InventarioFerreteria
binlog_expire_logs_seconds = 604800
max_binlog_size            = 100M
